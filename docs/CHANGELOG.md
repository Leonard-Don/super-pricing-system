# 更新日志

## Unreleased
- feat(alt-data): macro daily briefing composer —— Phase F5 把另类数据从「每组件单条 narrative」推进到「每日 1 页 5 段式宏观日报」。新增 `src/data/alternative/macro_briefing.py`：`MacroBriefing` frozen dataclass (`generated_at`, `time_window_days`, `policy_section`, `capital_flow_section`, `commodity_section`, `governance_section`, `composite_section`, `summary_paragraph`, `evidence_links`) + `compose_macro_briefing(manager, *, time_window_days=7)` 入口；synthesis 严格 deterministic（无 LLM，无网络），同输入同输出，所以 endpoint 端 `Cache-Control: max-age=300` 是安全的。每段最多 3 个 bullet：policy_section 排序 `policy_radar.industry_signals` 按 `|avg_impact|` 取阈值 ≥ 0.15 的 top 3 + 一个 policy_execution chaotic 部门补充行；capital_flow_section 三路汇流（fund_holdings 集中持有 ≥ 15 只基金 → 北向资金 ±2 亿行业级流入/流出 → 大宗交易承接/减持 top 行业）；commodity_section 去重 `macro_hf._history` 按 `(region, metal)`、SHFE 先 LME 后、跨区共振时加一行 "双侧去化/累积"；governance_section 列出 people_layer fragile companies `people_fragility_score ≥ 0.25`；composite_section 调用 `detect_composite_signals(manager, include_low=False)` 取前 3。`summary_paragraph` 优先级 `composite > policy > commodity > capital > governance` 选 3 段 theme 串成 3 句 "今日 alt-data 核心观察:" 段落，全空时退化 `EMPTY_BRIEFING_SUMMARY`。新端点 `GET /alt-data/macro-briefing?time_window_days=7` 返回完整 to_dict + `audit_doc_url`，`time_window_days` clamp `[1, 30]`。`scripts/export_public_summary.py` 新增 `_build_macro_briefing` 用 duck-typed stub manager 在磁盘 snapshots 上跑同一个 composer，往 `data/public/alt_data_summary.json` 注入 `macro_briefing.{summary_paragraph, top_3_themes, time_window_days, generated_at}`（**不**出库 evidence_links / snapshot paths）。前端新增 `frontend/src/components/GodEyeDashboard/MacroBriefingTile.jsx`（`data-testid="alt-data-macro-briefing-tile"`），挂在 `CompositeSignalTile` 与 `AltDataHealthTile` 之间，渲染 5 段 + per-section `[stale]` 证据胶囊 + 刷新按钮；`frontend/src/services/api/altDataAndMacro.js` 同步新增 `getAltDataMacroBriefing`。9 个 pytest 用例 (`tests/unit/test_macro_briefing.py`)：全 provider 覆盖、空 manager 优雅退化、部分 provider 优雅退化、`time_window_days` 透传 + clamp、idempotent、public summary 蒸馏只出 4 个安全字段、endpoint 形状 + Cache-Control、`time_window_days` 边界 422、`None` manager safe，全部通过。详见 `docs/alt_data_audit.md` § 19。
- feat(alt-data): composite signal history archive —— Phase F4.1 把 Phase F4 落地的复合信号检测结果按时间序列归档，与 Phase E4 的 narrative archive 同款打法。`src/data/alternative/composite_signal.py` 新增 `CompositeSignalArchive` / `ArchivedCompositeSignal`：每次调用 `/alt-data/composite-signals` 触发 `detect_composite_signals(manager)` 时，把检测出的全部 `CompositeSignal`（在 `min_conviction` / `direction` 过滤之前）通过 `O_APPEND + fsync` 追加到 `cache/alt_data/composite_signal_history.jsonl`，每行约 500-750 字节；文件超 10 MB 自动 `rename` 到 `composite_signal_history.jsonl.<UTC-iso>.archive` 并开始新文件；内存只缓存最近 100 条（narrative 是 200，复合信号 row 因为带 denormalised `supporting_components` 列表更重所以 cap 收紧），旧读流式落回磁盘。空检测结果（`append_many([])`）不入档，避免「本拍无信号」噪音淹没真实事件。新端点 `GET /alt-data/composite-signals/history?days=14&industry=<name>&min_conviction=<level>` 返回 `{archives, total, days_window, industry_scope, min_conviction, audit_doc_url}`，`days` 上限 90、`min_conviction` 走 `high > medium > low` 排序；前端 `CompositeSignalTile` 增加「查看历史」按钮 + Antd Drawer + Timeline（`data-testid="composite-signal-history-drawer"`），每行渲染 `archived_at` / target / 方向 tag / conviction stars / 支撑组件数。新增 10 个 pytest 用例 (`tests/unit/test_composite_signal_history.py`：append/recent roundtrip、days/industry/min_conviction 三类过滤、轮转、坏行容错、in-memory cap vs 磁盘 fallback、`append_many([])` 幂等、endpoint 形状 + days clamp、`/alt-data/composite-signals` 端点钩入归档)，全量 1337 passed + 5 skipped (baseline 1327 + 10 new)，mypy 72 / 73 baseline 未回归，ruff/pyflakes baseline 169 未回归，OpenAPI 仅 additive (新增 `/alt-data/composite-signals/history` 一个 GET，无 contract drift)。容量预估：busy 工作负载下 ~200 行/天 × 500-750 字节 ≈ 100-150 KB/天，30 天 ≈ 4 MB，90 天 ≈ 13 MB，因此 10 MB 阈值约 2 个月轮转一次。详见 `docs/alt_data_audit.md` § 18。
- feat(alt-data): cross-component composite signal detector —— Phase F4 把 9 个 alt-data 提供器从「并行存在」推进到「会聚合发声」。新增 `src/data/alternative/composite_signal.py`：核心入口 `detect_composite_signals(manager) → list[CompositeSignal]` 在 `manager.latest_signals` + `manager.providers[*]._history` 上做只读扫描，按行业聚合每个提供器的方向投票；当 ≥3 个独立提供器对同一行业指向同一方向时输出 `CompositeSignal{direction, target_kind="industry", target, conviction, supporting_components, emit_at, aggregate_strength}`，按 `(conviction desc → aggregate_strength desc → target asc)` 排序。8 个组件读取器各自落地组件层阈值：`policy_radar.industry_signals[X].avg_impact > +0.20` 视为看多、`< -0.20` 看空、`|impact| ≥ 0.30` 标 strong；`policy_execution` 取 policy_radar 的方向但 gate 在 `record_count > 0` 上避免空 feed 双重计数；`northbound.top_inflow_industries`/`top_outflow_industries` 的 industry-level netflow `≥ +2 亿`/`≤ -2 亿` 触发方向，`|netflow| ≥ 5 亿` 标 strong；`fund_holdings.top_concentration_tickers` 经 `_TICKER_INDUSTRY_FALLBACK` 反查到行业后聚合 `total_aum_weight_pct ≥ 0.35` 触发看多 (单边)、`≥ 0.55` 标 strong；`macro_hf` 用 `INDUSTRY_RELEVANT_METALS` 把行业映射到金属再按 `destocking/restocking` 投票，destocking 单边为看多、restocking 单边为看空、混合时 skip；`shfe_inventory` 复用同一逻辑但 filter 仅 SHFE region 让 live feed 与 LME proxy 不互相借力；`people_layer` 经 `supportive_companies`/`fragile_companies` 行业反查映射；`supply_chain` 通过 `_history` 上 industry tag/raw 命中后取平均 `normalized_score ≥ +0.15`/`≤ -0.15` 触发。Conviction 分三档：4+ 同向且大多 strong → HIGH，3 同向 → MEDIUM，2 同向 → LOW (默认过滤)；同行业两侧同时 ≥2 → conflict skip (`_build_conflict_summary` 才是分裂证据的对口表面)。新增 `GET /alt-data/composite-signals?min_conviction=medium&direction=bullish&limit=20`：返回 `composite_signals[]`、`tier_summary{high/medium/low}`、`public_summary{top_3_bullish, top_3_bearish}`、`snapshot_timestamp`，`Cache-Control: max-age=300`；OpenAPI 仅 additive。`scripts/export_public_summary.py` 新增 `_build_composite_signals` 在不引入 akshare/yfinance 的前提下用 duck-typed stub manager 在磁盘 snapshots 上跑同一个 detector，把 `composite_signals.{top_3_bullish, top_3_bearish, total_bullish, total_bearish}` 注入 `data/public/alt_data_summary.json` (schema_version 保持 1)。新增 `frontend/src/components/GodEyeDashboard/CompositeSignalTile.jsx` 挂到「另类数据与物理世界」的 `AltDataNarrativeTile` 与 `AltDataHealthTile` 之间，两栏 5+5 行渲染顶部看多 / 看空复合信号，每行 `industry + direction tag + conviction stars + 强度 + 逗号分隔支撑组件`，标题栏带 HIGH/MED/LOW 计数胶囊和审计文档直链；同步 `frontend/src/services/api/altDataAndMacro.js` 新增 `getCompositeSignals(params)` API。10 个 pytest 用例 (`tests/unit/test_composite_signal.py`)：6 全看多 → HIGH、3 同向 → MEDIUM、2v2 混合 → 无 emit、阈值边界 (`avg_impact=0.20` 包含)、空 manager → []、idempotent (同输入同输出)、`include_low` 切换 2 组件 emit、`top_3` 截断、endpoint `min_conviction=high/medium/low` 过滤 + 公共 summary 形状。全部通过；按真实磁盘 cache 跑一次 detector 当前输出 `1 bearish low-conviction for 新能源汽车` (policy_radar + policy_execution 同向 strong)；fund_holdings / northbound snapshots 待 live akshare 写盘后即可让 AI算力 / 电网 进入看多榜单。`docs/alt_data_audit.md` 新增 § 17。
- feat(alt-data): northbound flows provider —— Phase F3 把 `northbound` 作为第 9 个 alt-data sub-package 引入，提供日频公开披露的外资 (HSGT 沪深港通) 净流入信号，与刚落地的 `fund_holdings` (季频公募) 形成「外资 / 内资」双锚。新增 `src/data/alternative/northbound/` 二件套：`__init__.py` 导出 `NorthboundProvider`，`provider.py` 实现 `BaseAltDataProvider` 四阶段管线 —— `fetch` 串行调用三个 akshare HSGT 端点：`stock_hsgt_hist_em(symbol="北向资金")` 拉日频净流入历史 (默认 60 天窗口，给 30 天累计留 1.5× 边际)、`stock_hsgt_hold_stock_em(market=...,indicator="今日排行")` 拉北向 top-100 个股持仓 (`北向` market 失败时回退 `沪股通`)、`stock_hsgt_board_rank_em(symbol="北向资金增持行业板块排行", indicator="今日")` 拉行业排行；每条 slice 各自 try/except，单端点 HTTPError / 404 / KeyError 本地降级为空 payload，确保单 slice 失败不会拖垮整个 run。`parse` 把三条 slice 收敛为 `record_type` ∈ {`netflow_daily`, `top_holding_stock`, `industry_netflow_agg`} 的统一结构，coverage 按响应 slice 数 / 3 计算。`normalize` 每个 record 产出一条 `AltDataRecord(category=FOREIGN_CAPITAL_FLOW)`，metadata 携带 `source_mode="public_disclosure"`、`lag_days=1`、`category="foreign_capital_flow"`、`record_type`；daily / industry 行用各自的 max-abs 归一化到 [-0.95, 0.95]，top-holding 行 score 设 0 (snapshot 而非 flow，narrative only)。confidence 以 0.75 为上限按 coverage 衰减。`to_signal` 收敛为 `last_trade_date / daily_netflow_cny_billion / cumulative_30d_cny_billion / top_inflow_industries[:5] / top_outflow_industries[:5]`，sign 用 5 亿 deadband (`signal = ±1` 当 |latest| > 5 亿)，strength 在 |latest| ≥ 50 亿处饱和到 1.0。新增 `AltDataCategory.FOREIGN_CAPITAL_FLOW = "foreign_capital_flow"` 枚举。`AltDataScheduler.DEFAULT_INTERVALS_MINUTES["northbound"] = 60*12` (720 分钟，两拍一天 —— 中午抓盘中态势，盘后抓终值，正好赶上 T+1 morning 决策)，`backend/app/core/alt_data_tasks.py` 同步注册 Celery task `alt_data.refresh.northbound`，beat schedule 从 7 条扩到 8 条。`AltDataManager._build_default_providers` 把 provider 注册为第 7 个、`SOURCE_TIER_RULES` 加 `northbound:netflow_daily → (public_disclosure, 0.82)`、`northbound:industry_netflow_agg → (public_disclosure, 0.80)`、`northbound:top_holding_stock → (public_disclosure, 0.75)`、`DEFAULT_PROVIDER_CONFIG["northbound"]={"days_back": 60, "top_holding_limit": 100}`。`health_manifest.py` 加 WORKING-PROTOTYPE 行 (指向 `docs/alt_data_audit.md` § 16)，总数 8→9，working_prototype 5→6。`scripts/export_public_summary.py` 新增 `_distill_northbound` distiller：仅输出 `last_refresh_at / last_trade_date / daily_netflow_cny_billion / cumulative_30d_cny_billion / signal_strength / score / confidence / top_inflow_industries[:5] / top_outflow_industries[:5]`，**不**外泄 ticker / stock_name / holding_value_cny 等 per-stock 归因，`provider_info`、`refresh_status`、`records` 也不出库，schema_version 保持 1 (纯 additive)。`docs/alt_data_audit.md` § 2 verdict 表新增 northbound 行，§ 16 增 Phase F3 操作笔记。新增 9 个 pytest 用例 (`tests/unit/test_northbound_provider.py`：3-slice happy path + record_type 全覆盖、outflow day 处理 (direction=out, signal=-1)、3-slice 全失败 → low_coverage、partial-response (1/3 slice 响应) → partial_response=True、record schema 与 metadata 对齐 brief、history schema 异常优雅降级、industry 聚合排序正确、history/last_update 持久化、public summary distiller 字段过滤 + per-stock 不出库)，全部通过 `sys.modules` 桩 mock akshare，零 live HTTP；同步把 `test_alt_data_health.py` (total 8→9, WP 5→6)、`test_alt_data_tasks.py` (beat 7→8 条 + refresh callables 6→7) 与 `test_public_summary_export.py::test_components_health_aggregates_static_manifest` (total 8→9, working_prototype 5→6) 的计数器对齐。全量 1317 passed + 5 skipped (baseline 1308 + 9 new)，mypy / ruff baselines 未回归，OpenAPI 零 contract drift。详见 `docs/alt_data_audit.md` § 16。
- feat(alt-data): mutual fund holdings provider —— Phase F2 把 `fund_holdings` 作为第 8 个 alt-data sub-package 引入，提供季度披露的公募基金机构资金信号。新增 `src/data/alternative/fund_holdings/` 三件套：`fund_catalog.py` 维护 50-name 大型公募基金 catalog（六位代码 + 基金经理 + focus 标签，`CATALOG_VERSION="2026-Q1"`，季度手工刷新），`provider.py` 实现 `BaseAltDataProvider` 四阶段管线 —— `fetch` 顺序调用 `ak.fund_portfolio_hold_em(symbol=<code>, date=<year>)`（当前年 + 上一年回退）每只基金的十大持仓股；单只基金的 404 / HTTPError / KeyError 被本地捕获并降级为空 payload，绝不让一只基金拖垮整个 run。`parse` 把 N×10 的 (ticker, fund_code, weight_pct) 元组按 ticker 聚合为 `{holding_fund_count, total_aum_weight_pct, top_holder_fund_code, contributing_funds[:8]}`；`normalize` 每个 ticker 产出一条 `AltDataRecord(category=INSIDER_FLOW, record_type="fund_concentration_ticker")`，metadata 携带 `source_mode="public_disclosure"`、`lag_days=15`、`coverage`、`catalog_version`；confidence 以 0.7 为上限（让公开披露的季度滞后体感反映在加权证据里）按 coverage 线性衰减（30/50 → 0.42），少于 5 只响应的 run 标 `low_coverage=True`。`to_signal` 收敛成 `top_concentration_tickers` Top-10 leaderboard + 拥挤交易聚合度。`AltDataScheduler.DEFAULT_INTERVALS_MINUTES["fund_holdings"] = 60*24*7`（10080 分钟周频，与季度披露窗口的最坏噪声底吻合）；Celery beat `alt_data.refresh.fund_holdings` 在 `backend/app/core/alt_data_tasks.py` 与之并行注册，软/硬超时 240s/300s 抵御 50 次 akshare 顺序调用。`AltDataManager._build_default_providers` 把 provider 注册为第 6 个、`SOURCE_TIER_RULES` 加 `fund_holdings:concentration → (public_disclosure, 0.78)`、`DEFAULT_PROVIDER_CONFIG["fund_holdings"]={"top_holdings_limit": 10}`。`health_manifest.py` 加 WORKING-PROTOTYPE 行（指向 `docs/alt_data_audit.md` § 15）。`scripts/export_public_summary.py` 新增 `_distill_fund_holdings` distiller：仅输出 `last_refresh_at / total_funds_covered / total_funds_requested / catalog_version / signal_strength / score / confidence / top_concentration_tickers[:10]`，**不**外泄 `top_holder_fund_code` 等 per-fund 归因，`provider_info`、`refresh_status`、`records` 也照旧不出库；序列化前抹掉 `MAX_FUND_CONCENTRATION_TICKERS=10`。`docs/alt_data_audit.md` § 2 verdict 表新增一行，§ 15 增 Phase F2 操作笔记。新增 10 个 pytest 用例（`tests/unit/test_fund_holdings_provider.py`：catalog 形状下界、50×10 happy path、30/50 partial coverage → confidence<0.7、空响应 graceful degrade、record schema 与 metadata 对齐 brief、SH/sz/numeric ticker 归一化、未知列 schema 优雅降级、history 持久化、provider_info 暴露 catalog_version、public summary distiller 字段过滤），全量 1308 passed + 5 skipped（akshare 全程通过 `sys.modules` 桩 mock，零 live HTTP）。mypy / ruff baselines 未回归，OpenAPI 零 contract drift。详见 `docs/alt_data_audit.md` § 15。
- feat(alt-data): public summary export —— Phase F1 把私有的 `cache/alt_data/providers/*.json` runtime 缓存蒸馏为一份小而稳定、可安全提交的 `data/public/alt_data_summary.json`（参考快照下 ~5KB），供 sibling 项目 `cn-altdata-brief` 与未来的 GitHub Pages 日报通道直接读取。新增 `scripts/export_public_summary.py`：`build_public_summary` + `write_public_summary_atomic` 是源端，按 provider 应用专用 distiller（policy_radar 仅留 `total_records / policy_count / by_source / industry_signals`，by_source 强制回填 `fed/ecb/ndrc/nea/boe` 让暗源显式为 0；macro_hf 按金属聚合 `region_breakdown.{LME,SHFE}` 并优先取 SHFE live 读数；people_layer 仅留 `ticker_count / 脆弱-支持计数 / 均值 / watchlist_preview` 30 行的 `symbol/risk_level/stance/score`；policy_execution 仅留前 10 部门的 chaos 指标；supply_chain 仅留 `dimensions/alert_count`）。文件路径、原始 HTML、`_internal_*` debug 字段、`provider_info / refresh_status / records / latest_title / evidence` 等敏感 metadata 全部不出库。`json.dump(sort_keys=True, indent=2)` + `generated_at` 秒级 UTC ISO 戳保证相同输入字节级同输出，`git diff` 只看到真实数据变更而非元数据噪音；写盘走与 `governance.py` 同款 tempfile + atomic rename。`backend/app/core/alt_data_tasks.py` 新增 Celery 任务 `alt_data.export_public_summary`，每 30 分钟一拍（beat 条目 `alt-data-export-public-summary`，软/硬超时 45s/60s），任务体懒加载脚本模块复用 `export_public_summary`，CLI 与 beat 路径产出字节级一致的文件（除 `generated_at`）。`schema_version=1` 顶层；破坏性变更才 bump。`.gitignore` 把 `data/` 收紧为 `data/*` + `!data/public/` 让这一棵子树可提交。新增 12 个 pytest 用例（`tests/unit/test_public_summary_export.py`：5 个 provider key 全出 / 缺失 provider 不合成 / 空 `industry_signals` 用空 dict 回退 / `schema_version` 上浮 / atomic-write 一步换 + 不留 .tmp / 序列化失败保留旧文件 / 敏感字段不出库 / 同输入字节同输出 / by_source 暗源回填 0 / metals 多区域聚合 / `components_health` 镜射静态 manifest / end-to-end 写盘），并把 `test_alt_data_tasks.py` 的 beat 计数从 5 改为 6 + 加一个 export 任务 body delegation 测试。完整套件 1298 passed + 5 skipped 与基线持平；mypy 72 / baseline 73（继承 Phase E4），ruff/pyflakes baseline 169 不变，无 OpenAPI 改动。初始落盘文件 `data/public/alt_data_summary.json` 已生成（5054 字节）。详见 `docs/alt_data_audit.md` § 14。
- feat(alt-data): narrative history archive + viewer —— Phase E4 把 `/alt-data/narrative` 输出按时间序列归档。`src/data/alternative/narrative.py` 新增 `NarrativeArchive` / `ArchivedNarrative`：每次调用 `/alt-data/narrative`（含 `industry` 作用域）都会以 `O_APPEND` + `fsync` 追加一行到 `cache/alt_data/narrative_history.jsonl`（每条 ~300 字节），文件超 10MB 自动 rename 到 `narrative_history.jsonl.<utc-iso>.archive` 并开始新文件；内存仅缓存最近 200 条，旧读流式落回磁盘。空状态（bullets=[]）不入档。新端点 `GET /alt-data/narrative/history?days=14&industry=…`（默认 14，上限 90）返回 `{archives, total, days_window, industry_scope}`；前端 `AltDataNarrativeTile` 增加「查看历史」按钮 + Antd Drawer + Timeline，挂在卡片标题栏右侧懒加载。新增 8 个 pytest 用例（`tests/unit/test_alt_data_narrative_history.py`：roundtrip、days/industry 过滤、轮转、坏行容错、内存上限/磁盘 fallback、端点形状、append 钩入端点）和 3 个 Jest 用例（drawer 打开取回、空态、newest-first 排序保持）。OpenAPI 仅追加 `/alt-data/narrative/history` 一个 GET，零 breaking change；mypy/ruff baseline 不变。详见 `docs/alt_data_audit.md` § 13。
- feat(workbench): alt-data candidate task queue —— Phase E3 把另类数据从「仅可见」推进到「主动建议研究方向」。新增 `src/research/alt_data_candidates.py`：根据阈值（policy_radar `|avg_impact| ≥ 0.30` 且 `mentions ≥ 3`；SHFE 库存 `|weekly_change_pct| ≥ 5%`）从 `AltDataManager` 实时合成 `AltDataCandidate`，按 `(component, signal_type, industry)` 反复幂等去重；`CandidateStore` 以 `cache/workbench/alt_data_candidates.json` 原子写入持久化，调和规则保留用户已 `dismissed/snoozed/converted` 的状态，并把 30+ 天无信号的旧候选剪掉。后端在 `/research-workbench/alt-data-candidates*` 下新增 5 个端点（list / refresh / convert / dismiss / snooze）；Convert 直接调用 `ResearchWorkbenchStore.create_task` 生成 `macro_mispricing` 类型任务，`context.tags` 自动写入 `alt-data:<component>` 与 `industry:<name>`，证据快照随 `evidence_link` 透传。前端 `frontend/src/components/research-workbench/AltDataCandidateQueue.jsx` 渲染候选队列，挂在 `WorkbenchBoardSection` 之上、看板左列顶部，提供「转研究卡 / 忽略 / snooze 1h / snooze 24h」一键动作。新增 15 个 pytest 用例（`tests/unit/test_alt_data_candidates.py`）和 8 个 Jest 用例（`frontend/src/components/research-workbench/__tests__/AltDataCandidateQueue.test.jsx`），OpenAPI baseline 仅 additive（新增 `/research-workbench/alt-data-candidates*` 共 5 个端点并补齐响应 schema）。阈值通过 `ALT_DATA_CANDIDATE_POLICY_IMPACT_THRESHOLD` 等环境变量配置，未硬编码。详见 `docs/alt_data_audit.md` § 12。
- feat(pricing): alt-data industry context on Gap Analysis page —— Phase E2.1 把 GodEye 端的另类数据合成器接到定价研究主单股决策面。后端 `src/data/alternative/narrative.py:build_alt_data_narrative` 接受可选 `ticker_industry`：政策雷达过滤为标签命中行业的记录，宏观高频库存按 `INDUSTRY_RELEVANT_METALS`（新能源汽车→铜/铝/镍/锂，AI算力→铜，…）裁剪；行业无显著信号时返回 `本行业暂无显著另类数据信号` 退化文案。`GET /alt-data/narrative?industry=新能源汽车` 走行业作用域路径并在响应中回显 `industry_scope`。新增 `src/data/alternative/ticker_industry.py`（静态符号 → 行业映射 + 数据管理器兜底 + 行业→金属字典）。前端 `frontend/src/components/pricing/AltDataContextPanel.jsx` 懒加载挂到 `PricingResultsSection` 第二行（紧贴 CAPM/FF3/DCF 卡片）；`resolveAltDataIndustry` 把 valuation/sector 字段译成规范行业标签后传入 panel。新增 11 个 pytest 用例（`tests/unit/test_alt_data_narrative.py`，1222→1261）和 4 个 Jest 用例（`frontend/src/components/pricing/__tests__/AltDataContextPanel.test.jsx`）。OpenAPI 仅追加 query 参数（`industry`，可空，maxLength=64）+ 422 响应，无 breaking change。详见 `docs/alt_data_audit.md` § 11。
- feat(godseye): alt-data narrative tile —— Phase E2 在 GodEye 战场大屏「另类数据与物理世界」分区 `AltDataHealthTile` 之上新增 `AltDataNarrativeTile`，消费新落地的 `/alt-data/narrative` 端点。后端 `src/data/alternative/narrative.py` 严格确定性合成 2-3 句要点：句①政策雷达按源拆分 + 最高影响力行业（`avg_impact` + 偏多/偏空标签），句②LME/SHFE 库存按区域 destocking/restocking/stable，句③仅在上下文一致时生成的综合判读；陈旧（>7d）组件加 `[stale]` 前缀，每条要点附 `evidence_link`。端点带 `Cache-Control: max-age=300`。覆盖 8 个 pytest 用例（`tests/unit/test_alt_data_narrative.py`）和 7 个 Jest 用例（`__tests__/AltDataNarrativeTile.test.jsx`）。OpenAPI baseline 追加 `/alt-data/narrative`，diff 仅 additive。详见 `docs/alt_data_audit.md` § 11。
- feat(godseye): alt-data health tile —— 在 GodEye 战场大屏「另类数据与物理世界」分区新增 `AltDataHealthTile`，消费 Phase E1 新落地的 `/alt-data/health` 端点，渲染 7 个组件的 verdict、最近刷新相对时间与审计章节直链；附 4 项 PROD/WORKING/SCAFFOLDING/DEAD 统计卡和 9 个 Jest 用例（`frontend/src/components/GodEyeDashboard/AltDataHealthTile.jsx`、`__tests__/AltDataHealthTile.test.jsx`、`services/api/altDataAndMacro.js`）。

## v4.2.0 (2026-05-05)
- 结构整治版：原 7 项 v4.2.0 hot-spot + 3 个 endpoint 层 god-module（`analysis/routes` 921→325、`infrastructure` 818→321、`macro_quality/_summaries` 786→388）全部收敛到 600 行 CLAUDE.md 软上限以下；4 个 package `__init__` 死 re-export 清理
- `frontend/src/utils/researchTaskSignals.js` 1716→661，按 signal family 拆为 8 个文件
- 死代码扫荡：删 5 个废 src 模块（`async_utils` / `EnhancedMomentumStrategy` / `SentimentStrategy` 等）、14 个 orphan endpoint + 配套 service helper、7 个 frontend orphan 目录/文件、2 个 scratch script、9 个真未用依赖（`ta` / `aiofiles` / `APScheduler` / `prometheus-client` / `sphinx` 等）
- 质量门基线对齐 CI 实测：mypy 365→342、ruff/pyflakes 194→181，未来 regression 直接失败而非默默过线
- mypy `python_version` 与 CI runtime 对齐（3.9→3.13），消除 14 个 PEP 604 false positive；`/pricing/gap-history` E2E flake（45s yfinance timeout）通过 timeout profile 修复
- 净行数变化 -46,950；后端 520 测试 + 前端 188 Jest 全过；OpenAPI zero contract drift
- 详见 `docs/releases/v4.2.0.md`

## v4.1.0 (2026-04-19)
- 前后端深链入口进一步收口：`/quantlab`、`/pricing` 等路径别名现会规范回 `?view=` 视图 URL，刷新、分享和从工作台重开研究页时上下文更稳定
- Quant Lab 与 Research Workbench 完成一级页面骨架统一，新增 hero、context rail、workspace surface 与右侧焦点卡，桌面/移动端首屏层级更清晰且已补浏览器回归
- 告警编排的 `persist_event_record=false` 现具备真实“不持久化”语义，不再偷偷写入 `alert_event`、`alert_event_dispatch` 或编排历史记录
- 后端 UTC 时间调用完成清理，`datetime.utcnow()` 统一替换为时区安全 helper，并保持现有 UTC 存储契约不变

## v4.0.0 (2026-04-14)
- 基础设施层正式产品化：新增 `Infrastructure` API、认证令牌、持久化状态、Redis/Celery 任务队列、通知能力与 TimescaleDB schema，并补齐基础设施独立启动、迁移与健康检查脚本
- Quant Lab 升级为独立量化实验台，覆盖策略优化、批量回测、基准对比、组合实验、Walk-Forward、风险中心、交易日志、告警编排与估值实验等新工作流
- GodEye 从宏观错误定价扩展到结构性衰败与部门混乱监控，新增 people / governance / execution / physical / evidence 维度雷达、部门执行混乱看板、贸易论点跟踪与物理世界观测面板
- 研究运营链路继续深度模块化：Research Workbench、实时复盘、行业研究、跨市场分析与定价研究新增更多状态持久化、异步处理、复制分享与上下文切换能力

## v3.9.0 (2026-04-02)
- 将宏观错误定价正式扩展为 6 因子可靠度引擎，新增利率曲线压力、信用利差压力与汇率错配三类因子，并补齐冲突、覆盖、时滞、漂移、反转前兆、政策源健康度与输入可靠度诊断
- 定价研究完成后端支撑层与前端展示层模块化拆分，补齐标的检索、同行候选池、基准因子摘要、敏感性/估值支撑解释，让 CAPM / FF / DCF / Gap Analysis 的结果更稳定且更易扩展
- 实时行情页升级为“数据流 + 派生状态 + 分享模板”结构，复盘快照与时间线开始版本化管理，诊断开关、导出与分享文案更一致，相关 WebSocket / contract 测试同步补齐
- 行业研究与工作台继续产品化：行业自选/保存视图/提醒阈值支持后端同步与导入导出，行业成分股构建增加流式状态反馈，GodEye 与 Research Workbench 进一步拆分为独立模块

## v3.8.0 (2026-04-01)
- 将研究运营闭环从“状态提示”升级为“动作姿势”：GodEye、Cross-Market 与 Research Workbench 现已统一区分降级运行、自动降级、核心腿受压、复核语境切换与输入可靠度变化，并给出明确的下一步动作建议
- 为 pricing、backtest、realtime、industry 并行模块补齐统一的结果姿势与行动提示层，支持从结果页直接判断“推进执行清单 / 先复核假设 / 继续观察补证”
- 扩展实时复盘与提醒管理能力：复盘快照与时间线容量提升，提醒支持条件筛选、批量启停/重置/删除，实时详情页保留跨标的对比选择记忆
- 高级回测实验台继续模块化，模板管理、研究洞察、跨市场诊断与研究剧本联动更稳定，定价研究也新增“切换到跨市场验证”的判定与原因解释

## v3.7.0 (2026-03-27)
- 将另类数据与宏观因子升级为可追溯证据质量引擎，新增实体统一、来源可信度、冲突/漂移/断流/跨源确认、一致度、反转前兆与因子共振判断
- 政策公开源升级为官方 feed + 正文抓取 + source health 诊断，政策源退化现已直接影响宏观因子有效置信度与跨市场研究输入质量
- 跨市场模板推荐与回测解释补齐 selection quality、ranking penalty、bias compression、core leg pressure 与 auto-downgrade 逻辑，默认模板选择会主动避开受压主题
- GodEye 与 Research Workbench 打通研究运营闭环，支持共振驱动、政策源驱动、自动降级、核心腿受压等任务优先级、直达 deep link 与版本对比解释

## v3.6.0 (2026-03-20)
- 打通 GodEye、跨市场模板推荐与研究工作台，新增“主导叙事切换”预警与直达跨市场剧本入口
- 将宏观因子与另类数据正式映射为跨市场模板的动态权重偏置，回测结果开始区分模板原始权重与信号驱动后的有效权重
- 升级跨市场执行研究层，补齐 execution plan、集中度、lot efficiency、rebalance cadence 与 stress test 诊断
- 强化研究工作台的快照解释与版本对比，支持比较 recommendation、allocation mode、bias、dominant driver、theme core/support 等主题变化

## v3.5.0 (2026-03-18)
- 重建实时行情深度详情，恢复实时快照、多维分析与 AI 预测联动
- 优化实时行情与分析页前端加载速度，补齐懒加载、请求去重与测试回归
- 打通交易 WebSocket 快照与广播，交易弹窗开始直接消费单条实时 quote 与账户推送
- 提升回测结果兼容性与展示一致性，补齐历史、导出和前端工作台契约

## v3.4.1 (2026-03-18)
- 修复策略回测收益率归零、历史快照异常和策略对比失败问题
- 统一回测结果契约，补齐历史、报告、导出和前端展示字段兼容
- 升级策略回测工作台界面、图表与中文文案，并修复策略对比中文报告导出

## v3.4.0 (2026-03-17)
- 新增研究工作台，支持后端持久化研究任务卡
- 打通 GodEye、定价研究、跨市场回测之间的保存与重开闭环
- 增加研究剧本与工作台联动，形成可持续推进的研究流

## v3.3.0 (2026-03-16)
- 优化 GitHub 仓库首页展示与 README 信息结构
- 新增项目界面截图与本地体验引导
- 补充贡献指南并统一版本元数据

## v3.2.0 (2025-12-24)
- 回测与分析相关 API 稳定化
- 引入多数据源适配结构（providers）
- 新增回测历史与报告导出能力

## v3.1.0 (2025-09-09)
- 安全与代码质量修复
- 基础性能与缓存优化

## 说明
- 版本号以项目根目录 `VERSION` 文件为准
- 可运行 `python scripts/sync_version.py` 同步前端元数据
- 文档更新时间与代码版本可能不同步，以代码为准

---

**最后更新**: 2026-04-19
