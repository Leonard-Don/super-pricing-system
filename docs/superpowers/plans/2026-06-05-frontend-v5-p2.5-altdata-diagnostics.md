# 前端 v5 · P2.5 另类数据深度诊断 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 把 P2 推迟的 7 个自取数深度 alt-data 诊断瓦片做出来,接到 `/godeye` 一个"深度诊断"段落里。每个瓦片自取数(自己管 loading/error)。

**Architecture:** 复用 P0–P2 基座(`api`、`DataTable`、`ChartFrame`、暗金 tokens、shadcn、Recharts 主题、`@/features/godeye/lib` 已移植的 displayLabels/macroFactorColors 等)。新代码归 `web/src/features/godeye/components/`(诊断瓦片)+ 扩展 `web/src/services/api/altDataAndMacro.ts`。

**范围:** 仅这 7 个瓦片 + 它们的 API + GodeyePage 的"深度诊断"段。**不碰** workbench/P3 相关(由 P3 分支并行做)。

**7 个瓦片 → API(端点见下表):**
| 瓦片(旧 `frontend/src/components/GodEyeDashboard/`) | 行数 | API 方法 → 端点 |
|---|---|---|
| `AltDataHealthTile.jsx` | 252 | `getAltDataHealth` → GET /alt-data/health |
| `AltDataNarrativeTile.jsx` | 337 | `getAltDataNarrative` → GET /alt-data/narrative · `getAltDataNarrativeHistory` → GET /alt-data/narrative/history |
| `AltSignalDiagnosticsTile.jsx` | 307 | `getAltSignalDiagnostics` → GET /alt-data/diagnostics/signals |
| `CrossArchiveThemesTile.jsx` | 322 | `getAltDataCrossArchiveThemes` → GET /alt-data/cross-archive-themes |
| `CompositeSignalTile.jsx` | 396 | `getCompositeSignals` → GET /alt-data/composite-signals · `getCompositeSignalsClusterAware` → GET /alt-data/composite-signals-cluster-aware · `getCompositeSignalHistory` → GET /alt-data/composite-signals/history |
| `AltDataAdvancedDiagnosticsTile.jsx` | 437 | `getAltDataThemesWithDiversity` → GET /alt-data/themes-with-diversity · `getAltDataProviderCorrelation` → GET /alt-data/provider-correlation · `getCompositeSignalsClusterAware`(复用)· `getCompositeSignalComparison` → GET /alt-data/composite-signal-comparison |
| `MacroBriefingTile.jsx` | 628 | `getAltDataMacroBriefing` → GET /alt-data/macro-briefing · `getAltDataMacroBriefingDelta` → GET /alt-data/macro-briefing-delta · `getAltDataMacroBriefingHistory` → GET /alt-data/macro-briefing/history |

**通用约束(每个子代理都适用):**
- **工作目录是这个 worktree:`/Users/leonardodon/.sps-wt/p2.5`**(分支 `frontend-v5-p2.5-altdata`)。旧代码在主仓 `/Users/leonardodon/super-pricing-system/frontend/`(只读参考,行为权威)。**不改 `frontend/`,不碰别的 worktree。**
- **禁止 `npm run dev`(阻塞)。** 验证用 `npx tsc --noEmit`、`npx vitest run <file>`、`npm run build`(在本 worktree 的 `web/` 下)。
- **lint 看真退出码**:`npx eslint . ; echo "exit=$?"`(别 `| tail` 后读 `$?`)。
- **vitest 慢(60–170s),别当卡死。**
- 无 `any`(eslint 报错);精确类型或 `unknown`+窄化。
- Token 只用 shadcn 语义类 + `text-pos`/`text-neg` + `macroFactorColors` 常量。
- 自取数瓦片:用 `useEffect` + 自管 `loading/error/data`,API 走 `@/services/api/altDataAndMacro`。空/加载/错误态都要有。`<TooltipProvider>` 若用 tooltip 记得包。
- 每任务 `tsc`/`lint`/相关 `vitest`/`build` 全绿后 commit。

---

### Task 1: API 扩展 — 13 个诊断方法

**源:** `frontend/src/services/api/altDataAndMacro.js`(取上表的诊断方法)。
**Files:** 扩展 `web/src/services/api/altDataAndMacro.ts`(P2 已建,有 getMacroOverview 等;**追加不替换**)+ `__tests__/altDataDiagnostics.test.ts`
- [ ] 写测试(先失败):mock `@/services/api/core`,断言 `getAltDataHealth()` GET `/alt-data/health`、`getAltDataNarrative({industry})` GET `/alt-data/narrative`、`getCompositeSignalsClusterAware()` GET `/alt-data/composite-signals-cluster-aware`、`getAltDataMacroBriefing()` GET `/alt-data/macro-briefing`。
- [ ] 运行 → 失败。
- [ ] 追加 13 个方法(见上表),都用 `api`+`withTimeoutProfile('dashboard')`。类型尽量引 `@/generated/api-types`;否则 `unknown`+窄化。
- [ ] vitest + tsc + lint 绿。
- [ ] Commit:`feat(web): alt-data diagnostics API methods (13, typed)`

---

### Task 2: 瓦片组 A — Health + Narrative + CrossArchiveThemes

**源:** `AltDataHealthTile.jsx`(252)、`AltDataNarrativeTile.jsx`(337)、`CrossArchiveThemesTile.jsx`(322)。
**Files:** `web/src/features/godeye/components/{AltDataHealthTile,AltDataNarrativeTile,CrossArchiveThemesTile}.tsx` + `__tests__`(各一个渲染测试:mock 对应 API resolve 最小 payload,断言关键内容渲染 + 空/错误态)
- [ ] 写测试(先失败)→ 失败。
- [ ] 重建三个自取数瓦片(shadcn + 自取数):Health(provider PRODUCTION/PROTOTYPE/DEAD 裁决表)、Narrative(叙事摘要段 + bullet + provider verdict/freshness badge + 历史)、CrossArchive(跨档案长期主题信号)。读取路径以旧组件为准 + 窄类型。
- [ ] vitest + tsc + lint + build 绿。
- [ ] Commit:`feat(web): alt-data diagnostic tiles — health/narrative/cross-archive`

---

### Task 3: 瓦片组 B — Composite + AltSignalDiagnostics

**源:** `CompositeSignalTile.jsx`(396)、`AltSignalDiagnosticsTile.jsx`(307)。
**Files:** `web/src/features/godeye/components/{CompositeSignalTile,AltSignalDiagnosticsTile}.tsx` + `__tests__`
- [ ] 写测试(先失败)→ 失败。
- [ ] 重建:CompositeSignalTile(高置信复合信号 cluster-aware + 历史 sparkline,用 ChartFrame/Recharts)、AltSignalDiagnosticsTile(alt 信号诊断 + 半衰期衰减可视化)。自取数。
- [ ] vitest + tsc + lint + build 绿。
- [ ] Commit:`feat(web): alt-data diagnostic tiles — composite signals + signal diagnostics`

---

### Task 4: 瓦片 C — AdvancedDiagnostics

**源:** `AltDataAdvancedDiagnosticsTile.jsx`(437,最复杂:provider 相关性 + cluster-aware 信号 + 复合信号对比 + 主题多样性)。
**Files:** `web/src/features/godeye/components/AltDataAdvancedDiagnosticsTile.tsx` + `__tests__`
- [ ] 写测试(先失败)→ 失败。
- [ ] 重建(自取数,4 个子查询;表/图用 DataTable/ChartFrame)。按子区拆内部小组件别堆一个巨函数。
- [ ] vitest + tsc + lint + build 绿。
- [ ] Commit:`feat(web): alt-data advanced diagnostics tile`

---

### Task 5: 瓦片 D — MacroBriefing

**源:** `MacroBriefingTile.jsx`(628,最大:5 段宏观简报 + delta + 历史)。
**Files:** `web/src/features/godeye/components/MacroBriefingTile.tsx`(+ 必要的小 hook/子组件)+ `__tests__`
- [ ] 写测试(先失败)→ 失败。
- [ ] 重建(自取数:briefing + delta + history)。5 段简报分块渲染;历史用 drawer/sheet 或折叠。按段拆内部组件。
- [ ] vitest + tsc + lint + build 绿。
- [ ] Commit:`feat(web): macro briefing tile`

---

### Task 6: GodeyePage 接入"深度诊断"段 + 收尾门禁

**Files:** 改 `web/src/routes/godeye/GodeyePage.tsx`(在末尾加一个 `深度诊断` SectionBlock,渲染这 7 个自取数瓦片;它们自取数,不依赖 hook)。**只在文件末尾追加一段,尽量不动现有段落**(减少与 P3 的合并冲突面)。
- [ ] 写/扩展测试:GodeyePage 测试断言"深度诊断"段 kicker 渲染(瓦片自取数,可 mock 其 API 或断言段落容器)。
- [ ] 在 GodeyePage 末尾追加深度诊断段,渲染 7 瓦片(用 `<TooltipProvider>` 包裹若需要)。
- [ ] 全量门禁:`npx tsc --noEmit && npm run lint && npm test && npm run build`(真退出码,逐条绿)。
- [ ] 确认旧 `frontend/` 未改。
- [ ] Commit:`feat(web): godeye deep-diagnostics section (7 self-fetching alt-data tiles)`

---

## 自检
- 7 瓦片 + API + 接入全覆盖;每瓦片自取数、空/加载/错误态齐。
- 不碰 workbench/P3 文件;GodeyePage 只在末尾追加段(降低与 P3 分支的冲突)。
