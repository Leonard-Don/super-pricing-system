# 前端 v5 · P3.5 工作台简报/候选/批量/拖拽 Implementation Plan

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 补齐 P3 推迟的工作台功能:每日简报集群、alt-data 候选队列、批量操作、看板拖拽重排。

**Architecture:** 复用 P0–P3 基座 + `@/features/workbench/*`(P3 已建)+ `@/features/godeye/lib/*`。新代码归 `web/src/features/workbench/`,扩展 `research.ts`、`WorkbenchPage`、`useResearchWorkbenchData`、`WorkbenchBoard`。

**通用约束:**
- **工作目录:worktree `/Users/leonardodon/.sps-wt/p3.5`**(分支 `frontend-v5-p3.5-workbench-briefing`)。旧码只读参考 `/Users/leonardodon/super-pricing-system/frontend/`。**不改 `frontend/`,不碰别的 worktree。**
- **禁止 `npm run dev`。** 验证(本 worktree `web/`):`npx tsc --noEmit`、`npx vitest run <file>`、`npx eslint . ; echo exit=$?`(真退出码)、`npm run build`。vitest 慢(60–170s)。无 `any`。shadcn 语义 token + text-pos/neg。每任务全绿后 commit。
- **后端响应是 `success_response` 信封 `{success,data,error}`** —— research/briefing/candidate 端点的 service 方法 `return response.data`(= 信封),消费方读 `.data` 取 payload(与 P3 一致,别去掉 `.data`)。
- 复用 P3 已加的 `research.ts` 方法;`bulkUpdateResearchTasks`、`reorderResearchBoard` P3 已加(T2)。

---

### Task 1: API 扩展 — 简报分发 + 候选 + infra status

**源:** `frontend/src/services/api/research.js`(briefing + candidate 子集)、`infrastructure.js`(getInfrastructureStatus)。
**Files:** 扩展 `web/src/services/api/research.ts` + 新建/扩展 `web/src/services/api/infrastructure.ts` + `__tests__/researchBriefing.test.ts`
- [ ] 测试先行:mock core,断言 `getResearchBriefingDistribution()` GET `/research-workbench/briefing/distribution`、`sendResearchBriefing({})` POST `.../briefing/send`、`listAltDataCandidates()` GET `/research-workbench/alt-data-candidates`、`convertAltDataCandidate('id')` POST `.../alt-data-candidates/id/convert`。
- [ ] 实现:`getResearchBriefingDistribution`、`updateResearchBriefingDistribution`(PUT)、`runResearchBriefingDryRun`、`sendResearchBriefing`、`listAltDataCandidates`、`refreshAltDataCandidates`、`convertAltDataCandidate`、`dismissAltDataCandidate`、`snoozeAltDataCandidate`;`infrastructure.ts` 的 `getInfrastructureStatus`。`api`+`withTimeoutProfile('workbench')`。类型引 api-types 或 `unknown`+窄化。
- [ ] vitest+tsc+lint 绿。Commit:`feat(web): research briefing + alt-data-candidate API methods`

---

### Task 2: 纯逻辑移植 — 每日简报 builders（含 DOMPurify 修复）

**源:** `frontend/src/components/research-workbench/{_dailyBriefing.js(444),dailyBriefingHelpers.js(294)}` + `frontend/src/utils/realtimeShareTemplates.js`(escapeHtml)。
**Files:** `web/src/features/workbench/lib/{dailyBriefing,dailyBriefingHelpers}.ts` + `__tests__/dailyBriefing.test.ts`;`npm i dompurify @types/dompurify`
- [ ] 测试先行:`buildWorkbenchDailyBriefingText`/`...Markdown`/`...EmailHtml` 给定 payload 返回含关键字段的字符串;`buildWorkbenchDailyBriefingFilename` 返回带日期的名。
- [ ] 移植转 TS(纯函数,co-port `escapeHtml`)。**安全:`mountDailyBriefingShareContainer` 的 `innerHTML` 赋值前用 `DOMPurify.sanitize(...)`**(评估点名的 XSS 点)。
- [ ] vitest+tsc+lint 绿。Commit:`feat(web): port daily-briefing builders to TS (DOMPurify-sanitized share HTML)`

---

### Task 3: 简报 hooks

**源:** `useDailyBriefing.js`(572)、`useDailyBriefingSharing.js`(425)。
**Files:** `web/src/features/workbench/hooks/{useDailyBriefing,useDailyBriefingSharing}.ts` + `__tests__/useDailyBriefing.test.tsx`;`npm i jspdf`(分享 PDF)
- [ ] 测试先行:`renderHook` mock briefing API,断言 distribution 加载、预设 add/save/delete、dry-run/send 调用。
- [ ] 移植:useDailyBriefing(分发配置 state + 预设 CRUD + dry-run/send,API 走 research.ts)、useDailyBriefingSharing(组装 artifacts via dailyBriefing lib + clipboard/下载/jsPDF 导出)。react-hooks v7 用 startTransition/惰性初始化。无 `any`。
- [ ] vitest+tsc+lint 绿。Commit:`feat(web): port daily-briefing hooks (distribution + sharing/PDF)`

---

### Task 4: 简报 UI + 接入

**源:** `DailyBriefingPreviewDrawer.js`(109) + `WorkbenchOverviewPanels.js` 的简报控件部分。
**Files:** `web/src/features/workbench/components/{DailyBriefingPanel,DailyBriefingPreviewDrawer}.tsx` + 改 `web/src/routes/workbench/WorkbenchPage.tsx` + `__tests__`
- [ ] 测试先行:渲染 DailyBriefingPanel(分发配置 + 预设 + dry-run/send 按钮 + 预览触发);PreviewDrawer 用 shadcn `Sheet` + `<iframe srcDoc sandbox>`。
- [ ] 实现 + 接入 WorkbenchPage(简报作为 overview 区一块或 Sheet)。
- [ ] vitest+tsc+lint+build 绿。Commit:`feat(web): daily-briefing panel + preview drawer wired into workbench`

---

### Task 5: alt-data 候选队列

**源:** `AltDataCandidateQueue.jsx`(344)。
**Files:** `web/src/features/workbench/components/AltDataCandidateQueue.tsx` + `web/src/features/workbench/hooks/useAltDataCandidates.ts` + 改 WorkbenchPage + `__tests__`
- [ ] 测试先行:hook mock list/convert/dismiss/snooze;组件渲染候选 + convert/dismiss/snooze 按钮调对应回调。
- [ ] 实现:hook(list + refresh + convert/dismiss/snooze)+ 组件(候选列表,看板上方)+ 接入 WorkbenchPage。
- [ ] vitest+tsc+lint+build 绿。Commit:`feat(web): alt-data candidate queue (convert/dismiss/snooze)`

---

### Task 6: 批量操作 + 看板拖拽重排

**源:** `WorkbenchBoardSection.js` 的拖拽 + `WorkbenchOverviewPanels` 批量。`bulkUpdateResearchTasks`/`reorderResearchBoard` 已在 research.ts(P3)。
**Files:** 改 `web/src/features/workbench/components/WorkbenchBoard.tsx` + `useResearchWorkbenchData.ts`(接 bulk + reorder)+ `__tests__`;拖拽用原生 HTML5 DnD(draggable + onDragOver/onDrop),`reorderResearchBoard` 持久化;避免引重型 dnd 库。
- [ ] 测试先行:bulk select + bulk status 调 bulkUpdateResearchTasks;拖卡到另一列调 reorder/status。
- [ ] 实现(在 P3 留的 `// TODO (P3.5)` 处接上)。无 `any`。
- [ ] vitest+tsc+lint+build 绿。Commit:`feat(web): workbench bulk actions + drag/drop board reorder`

---

### Task 7: 收尾门禁 + 复审
- [ ] 全量 `npx tsc --noEmit && npm run lint && npm test && npm run build` 真退出码逐条绿。
- [ ] 旧 `frontend/` 未改。Commit(若有):`chore(web): P3.5 polish + green gate`

## 自检
- 简报/候选/批量/拖拽全覆盖;DOMPurify 修了 XSS 点;复用 P3 的 research API + workbench 组件;`.data` 信封读法保持。
