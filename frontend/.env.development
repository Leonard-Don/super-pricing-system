# 本地开发配置（仅 `npm start` 生效，不影响生产构建）。
# CRA 默认端口是 3000，但 README / 后端 CORS 默认 / OAuth 回调都以 3100 为准，
# 这里把开发端口固定到 3100，保证 `npm start` + 后端 开箱即可联通。
PORT=3100

# 后端 API 地址（与 backend/main.py 的 8100 对齐）。
# 变量名须与 src/services/api/core.js 读取的 REACT_APP_API_URL 一致；
# 不设时 core.js 默认回退到 http://127.0.0.1:8100，这里显式声明以固化开发契约。
REACT_APP_API_URL=http://127.0.0.1:8100
